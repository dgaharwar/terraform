# Same ERB pattern as customer repo — values come from Morpheus customOptions, not -var flags.
cloudSelector = "<%=customOptions['cloudSelector']%>"
groupSelector = "<%=customOptions['groupSelector']%>"
